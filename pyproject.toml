[project]
name = "labview"
version = "0.1.0"
description = "Terminal viewer for Lab HTML reports"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
labview = "labview.cli:main"

[build-system]
requires = ["uv_build>=0.9.28,<0.10.0"]
build-backend = "uv_build"
